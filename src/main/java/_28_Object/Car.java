package _28_Object;

public class Car {
    private String model;
    private String color;
    private int year;

    public Car(String model, String color, int year) {
        this.model = model;
        this.color = color;
        this.year = year;
    }

    @Override

    // 생성자 , getter, setter, toString, equals
    // 기계적으로 필드만 있으면 찍어낼 수 있는 메서드들 -> 보일러프레이트 코드
    // ->
}
